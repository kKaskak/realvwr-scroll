import { Mesh } from 'three';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
import { IDisposable, IMaterial } from 'webgi/interfaces';
export declare class MaterialPreviewGenerator implements IDisposable {
    viewer: ViewerApp;
    private _scene;
    private _channel;
    private _lights;
    constructor(viewer: ViewerApp);
    dispose(): void;
    shapes: Record<string, Mesh>;
    generate(material: IMaterial, shape?: string): string;
}
//# sourceMappingURL=MaterialPreviewGenerator.d.ts.map