import { SimpleEventDispatcher } from '../helpers/EventDispatcher';
import { ITexture, IViewerPlugin } from 'webgi/interfaces';
import { Vector4Tuple } from 'three';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
export declare class DebugPlugin extends SimpleEventDispatcher<''> implements IViewerPlugin {
    dirty: boolean;
    static readonly PluginType = "debug";
    private _viewer?;
    counters: Record<string, number>;
    onAdded(viewer: ViewerApp): Promise<void>;
    private _generators;
    addTexture(key: string, generator: () => ITexture | undefined, rect?: Vector4Tuple, texelModifier?: string, fragmentShader?: string, event?: string, isCubeTexture?: boolean): void;
    removeTexture(key: string, event?: string): void;
    private _preRender;
    private _postRender;
    onDispose(viewer: ViewerApp): Promise<void>;
    onRemove(viewer: ViewerApp): Promise<void>;
    private _showDebug;
}
//# sourceMappingURL=DebugPlugin.d.ts.map