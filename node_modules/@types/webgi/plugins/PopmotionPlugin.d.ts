import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
import { Driver } from 'popmotion/lib/animations/types';
import { AnimationOptions } from 'popmotion';
export interface AnimationResult {
    id: string;
    promise: Promise<string>;
    options: AnimationOptions<any>;
    stop: () => void;
    _stop?: () => void;
}
export declare class PopmotionPlugin extends AViewerPlugin<''> {
    static readonly PluginType = "PopmotionPlugin";
    enabled: boolean;
    toJSON: any;
    fromJSON: any;
    constructor(enabled?: boolean);
    private _lastFrameTime;
    private _updaters;
    dependencies: never[];
    private _fadeDisabled;
    /**
     * Disable the frame fade plugin while animation is running
     */
    disableFrameFade: boolean;
    private _postFrame;
    readonly defaultDriver: Driver;
    onAdded(viewer: ViewerApp): Promise<void>;
    onRemove(viewer: ViewerApp): Promise<void>;
    readonly animations: Record<string, AnimationResult>;
    animate<V>(options: AnimationOptions<V>): AnimationResult;
    animateAsync<V>(options: AnimationOptions<V>): Promise<string>;
}
//# sourceMappingURL=PopmotionPlugin.d.ts.map