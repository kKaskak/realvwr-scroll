import { ShaderPass2 } from 'webgi/passes/threejs/ShaderPass2';
import { IPass } from 'webgi/interfaces';
import { WebGLRenderer, WebGLRenderTarget } from 'three';
export declare class BilateralFilterPass extends ShaderPass2 implements IPass {
    private _target;
    smoothEnabled: boolean;
    edgeSharpness: number;
    constructor(target: WebGLRenderTarget, gBufferUnpack: string, sourceAccessor?: string);
    render(renderer: WebGLRenderer, writeBuffer: WebGLRenderTarget, readBuffer: WebGLRenderTarget, deltaTime: number, maskActive: boolean): void;
}
//# sourceMappingURL=BilateralFilterPass.d.ts.map