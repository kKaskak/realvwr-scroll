import { Camera, Material, Scene, WebGLRenderer, WebGLRenderTarget } from 'three';
import { RenderPass } from 'three/examples/jsm/postprocessing/RenderPass';
import { UiObjectConfig } from 'webgi/interfaces';
export declare class NormalRenderPass extends RenderPass {
    enabled: boolean;
    constructor(scene?: Scene, camera?: Camera, overrideMaterial?: Material);
    private _firstCall;
    render(renderer: WebGLRenderer, writeBuffer: WebGLRenderTarget, readBuffer: WebGLRenderTarget, deltaTime: number, maskActive: boolean): void;
    uiConfig?: UiObjectConfig;
}
//# sourceMappingURL=NormalRenderPass.d.ts.map