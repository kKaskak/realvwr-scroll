import { ShaderPass2 } from 'webgi/passes/threejs/ShaderPass2';
import { IRenderer, IRenderTarget, UiObjectConfig } from 'webgi/interfaces';
import { Material, WebGLRenderer, WebGLRenderTarget } from 'three';
import { MaterialExtension } from 'webgi/extras/asset_manager/threejs/MaterialExtender';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
export declare function addSSBevel(material: Material): boolean;
export declare class SSBevelPass extends ShaderPass2 {
    uiConfig?: UiObjectConfig;
    private _target;
    private _edgeMaterial;
    private _separableBlurMaterial;
    private _viewerApp;
    constructor(renderer: IRenderer, target: IRenderTarget, gBufferUnpack: string, v: ViewerApp);
    render(renderer: WebGLRenderer, writeBuffer: WebGLRenderTarget, readBuffer: WebGLRenderTarget, deltaTime: number, maskActive: boolean): void;
    private _initsamples;
    private _blurEdges;
    private _renderEdges;
    readonly materialExtension: MaterialExtension;
}
//# sourceMappingURL=SSBevelPass.d.ts.map