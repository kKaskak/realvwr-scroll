import { Loader, LoadingManager, Texture } from 'three';
export declare class EnvMapLoaderThree extends Loader {
    private _cubeTextures;
    private _lightDirection;
    private _size;
    private _numLod;
    private _encoding;
    private _type;
    private _format;
    private _cubeTexture?;
    hasShaderTextureLod: boolean;
    constructor(manager: LoadingManager);
    load(url: string, onLoad?: (t: Texture) => void, onProgress?: (event: ProgressEvent) => void, onError?: (event: ErrorEvent) => void): void;
    private _parseHeader;
    private _parseBufferData;
    private _convertToRGBABuffer;
    private _parseData;
}
//# sourceMappingURL=EnvMapLoaderThree.d.ts.map