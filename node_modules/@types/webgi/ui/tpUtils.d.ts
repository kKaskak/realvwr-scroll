import { BladeApi, FolderApi } from 'tweakpane';
import { BladeController, View } from '@tweakpane/core';
/**
 * Check if child.parent === parent
 * @param child
 * @param parent
 */
export declare const tweakpaneCheckParent: (child: BladeApi<BladeController<View>>, parent: FolderApi) => boolean;
/**
 * Move to a new parent or change index in the same parent.
 * @param child
 * @param newParent
 * @param index
 */
export declare const tweakPaneMoveToParentIndex: (child: BladeApi<BladeController<View>>, newParent: FolderApi, index: number) => boolean;
//# sourceMappingURL=tpUtils.d.ts.map